[pytest]
markers =
    integration: marks tests that make real HTTP requests to DailyMed API (deselect with '-m "not integration"')

# Automatically run doctests
addopts = --doctest-modules

# Log configuration
log_cli = True
log_cli_level = INFO
log_format = %(asctime)s [%(levelname)s] %(name)s: %(message)s
